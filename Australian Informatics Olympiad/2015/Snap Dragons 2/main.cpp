#include <bits/stdc++.h>
#include <fstream>
using namespace std;

int main(){
    ifstream infile; 
    infile.open("snapin.txt"); 
    ofstream outfile;
    outfile.open("snapout.txt");
    
    int R, C, rr, cr, rs, cs;
    infile >> R >> C >> rr >> cr >> rs >> cs

    infile.close();
    outfile.close();
    return 0;
}
