package cyberthon.cat;

public class Source {
    public static int keytoyourkingdom() {
        while (true) {
            boolean z = MainActivity.step;
            if (z) {
                MainActivity.step = z;
                do {
                } while (!MainActivity.step);
                MainActivity.step = false;
                do {
                } while (!MainActivity.step);
                MainActivity.step = false;
                do {
                } while (!MainActivity.step);
                MainActivity.step = false;
            }
        }
    }
}
