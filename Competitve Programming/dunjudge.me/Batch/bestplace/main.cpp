#include <bits/stdc++.h>
#include <fstream>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    ifstream infile; 
    infile.open("probein.txt"); 
    ofstream outfile;
    outfile.open("probeout.txt");


    infile.close();
    outfile.close();
    return 0;
}
