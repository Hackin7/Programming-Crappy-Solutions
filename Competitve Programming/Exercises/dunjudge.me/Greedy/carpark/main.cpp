#include <bits/stdc++.h>
#include <fstream>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int N,S; cin>>N>>S;
    int A[N];for(int i=0;i<N;i++){cin>>A[i];}
    
    vector<int> no;
    int filled = 0;
    for(auto i:A){
        
    }
    return 0;
}
