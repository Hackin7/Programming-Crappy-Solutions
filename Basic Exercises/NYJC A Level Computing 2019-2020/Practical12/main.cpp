#include <bits/stdc++.h>
#include <fstream>
using namespace std;

int main(){
    ifstream infile; 
    infile.open("probein.txt"); 
    ofstream outfile;
    outfile.open("probeout.txt");


    infile.close();
    outfile.close();
    return 0;
}
